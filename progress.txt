## Codebase Patterns
- Use `python-docx` `Document` with `BytesIO` buffer for in-memory DOCX generation
- Use `fpdf2` subclassing `FPDF` for PDF generation; prefer `cell()` with `new_x="LMARGIN"` / `new_y="NEXT"` over `multi_cell()` for simple text
- When registering a new API router in `main.py`, add both the import and `app.include_router()` call
- Export module follows: define exporter in `src/export/`, add endpoint in `src/api/export.py`, add tests in `tests/`
- Gamification components live in `dashboard/src/components/gamification/`; add proxy rewrite in `next.config.js` for any new gamification API routes

## [2026-05-23 15:58] - Phase 4: PDF/DOCX Export
- Implemented PDF/DOCX export for quizzes and lesson plans
- Created `src/export/` module with `docx_exporter.py` and `pdf_exporter.py`
- Added `GET /export/quiz/{id}?format=docx|pdf` and `GET /export/lesson-plan/{id}?format=docx|pdf` endpoints
- Added 13 tests (8 unit + 5 integration) in `tests/test_export.py`
- Added `fpdf2` dependency for PDF generation
- Added `src/export/AGENTS.md` with module patterns
- Updated project-level `AGENTS.md` with export module section
- Updated `02-Execution/Execution.md` with completion status

## [2026-05-23 16:20] - ST-004: Misconception Detection (verified existing)
- Verified ST-004 already implemented in commit 5f795a4
- Reviewed: `detect_misconception()` function in both `src/agents/tutor.py` and `src/graph/nodes/tutor.py`
- Reviewed: Heuristic keyword-based detection using `MISCONCEPTION_INDICATORS` list
- Reviewed: Misconception correction surfaced in Telegram bot responses (`handle_question`, `ask_command`, `hint_command`, `handle_hint`)
- Reviewed: Fields wired through `GraphOutput` in `orchestrator.py`
- All 6 misconception tests pass (28/28 agent tests, 55/56 total tests — pre-existing ChromaDB failure)
- All stories complete: ST-001 (Socratic) ✅ ST-003 (Hint Progression) ✅ ST-004 (Misconception Detection) ✅ ST-005 (Reveal Answer) ✅

## [2026-05-24 12:00] - GM-006: Dashboard Gamification Widgets
- Created 5 gamification components in `dashboard/src/components/gamification/`:
  - `XpCard.tsx` — XP card with level display and progress bar
  - `StreakWidget.tsx` — Streak display with fire icon, current/longest days
  - `MasteryProgressBar.tsx` — Level progress bar with gradient fill
  - `AchievementPanel.tsx` — Achievement badges grid (unlocked/locked states)
  - `GamificationProfile.tsx` — Combined widget that fetches from `/gamification/profile/{user_id}`
- Added `/gamification/:path*` rewrite in `dashboard/next.config.js`
- Updated `dashboard/src/app/students/[id]/page.tsx` to show gamification widgets alongside existing quiz stats
- Added Dashboard Gamification Widgets section to project `AGENTS.md` with usage patterns
- **Learnings for future iterations:**
  - Patterns discovered: Drop `<GamificationProfile userId={id} />` into any page with a user ID
  - Achievement definitions must be duplicated in frontend (for locked/unlocked display) and kept in sync with backend `ACHIEVEMENT_DEFINITIONS` in `src/api/gamification.py`
  - Backend `/gamification/profile/{user_id}` endpoint is fully built — GM-006 was purely frontend work
  - Dashboard TypeScript typecheck: `npx tsc --noEmit` in `dashboard/`
  - Dashboard build: `npx next build` in `dashboard/`
  - Backend test filter: `pytest tests/ -v -k "not test_chat_endpoint and not test_quiz_generate_endpoint and not test_quiz_generation"`
---
## [2026-05-24 21:30] - GM-008: Recovery Plan Rewards
- Added `RecoveryPlan` and `RecoveryTask` database models in `src/database/models.py`
- Added `recovery_task_completion` (40 XP) and `recovery_milestone` (50 XP bonus) to `XP_SOURCES`
- Added `RECOVERY_MILESTONE_THRESHOLDS` (3/5/10/15 tasks) for bonus milestone rewards
- Created `src/schemas/recovery.py` with `RecoveryPlanResponse`, `RecoveryTaskResponse`, `CompleteTaskResponse`, `CreateRecoveryPlanRequest` schemas
- Created `src/api/recovery.py` with three endpoints: POST /recovery/plan, GET /recovery/plan/{user_id}, POST /recovery/task/complete
- The task completion endpoint awards XP via `award_xp()`, checks milestones, updates streak, and checks achievements
- Added `recovery_progress` field to `GamificationProfileResponse` and wired into `_build_profile()` to query active plans per user
- Created `RecoveryProgressCard.tsx` frontend component in `dashboard/src/components/gamification/`
- Added `RecoveryProgressCard` to `GamificationProfile.tsx` render and TypeScript interface
- Registered recovery router in `src/main.py`
- Added 8 tests in `tests/test_recovery.py` covering XP sources, milestone thresholds, schema fields, progress response, and profile integration
- Added Recovery Plan Module section to project `AGENTS.md`
- All 23 gamification+recovery tests pass; 73/96 full suite pass (6 pre-existing telegram bot failures)
- **Learnings for future iterations:**
  - Patterns discovered: Follow `_save_quiz_rewards` pattern for any new XP source in the bot
  - Milestone rewards pattern: Define thresholds dict in gamification.py, check `completed in THRESHOLDS` in API handler, call `award_xp()` with milestone source
  - The `_build_profile()` function in `gamification.py` is the central place to add new data to the gamification profile response
  - Recovery plans don't need AI generation — they are simple remediation task trackers
  - XP wiring pattern is consistent: `award_xp()` → `update_streak()` → `check_achievements()` → `session.commit()`
---
- Added XP fields (xp_awarded, level_up, new_level) to TutorResponse and GraphChatResponse schemas
- Wired `award_xp()` for `tutor_interaction` (5 XP per XP_SOURCES) into POST /chat endpoint
- Wired `award_xp()` for `tutor_interaction` into POST /graph/chat endpoint (added Depends(get_session))
- Added `_save_tutor_rewards()` helper in Telegram bot (follows `_save_quiz_rewards` pattern)
- Wired XP notifications into bot's `ask_command` and `handle_question` handlers
- Added 3 tests: tutor_interaction XP source, TutorResponse XP fields, GraphChatResponse XP fields
- **Learnings for future iterations:**
  - Patterns discovered: Follow `_save_quiz_rewards` pattern for any new XP source in the bot; `_save_tutor_rewards` is the template for bot-side reward integration
  - When adding a new API endpoint that needs gamification, use `Depends(get_session)` and then call `award_xp()`, `update_streak()`, `check_achievements()`
  - The `POST /graph/chat` endpoint did not previously have DB access added — needed both the session dependency and the gamification calls
  - Bot tutor handlers pass `user_id=None` to `TutorAgent.answer()`, so XP must be awarded separately via `_save_tutor_rewards` which looks up user by telegram_id
  - Gamification XP definitions in `XP_SOURCES` dict serve as the "configurable" reward triggers — no migration needed
---
