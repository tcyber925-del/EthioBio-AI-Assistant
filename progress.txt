## Codebase Patterns
- Use `python-docx` `Document` with `BytesIO` buffer for in-memory DOCX generation
- Use `fpdf2` subclassing `FPDF` for PDF generation; prefer `cell()` with `new_x="LMARGIN"` / `new_y="NEXT"` over `multi_cell()` for simple text
- When registering a new API router in `main.py`, add both the import and `app.include_router()` call
- Export module follows: define exporter in `src/export/`, add endpoint in `src/api/export.py`, add tests in `tests/`
- Database models follow `QuizAttempt` pattern: UUID PK, FK to users, standard timestamp fields
- No migration needed — `Base.metadata.create_all` in `init_db()` handles table creation
- Schema files follow `SchemaModel` base from `src/schemas/base.py`
- The Ralph system stores the active PRD at `scripts/ralph/prd.json` and archives old ones
- Dashboard API rewrites go in `dashboard/next.config.js` under the `async rewrites()` section — add the path before committing
- Dashboard pages follow patterns: `'use client'`, `fetchWithTimeout` from `@/lib/fetch`, lucide-react icons, Tailwind CSS custom theme (bg-card, border-border, etc.), `useState` for state management

## [2026-05-23 15:58] - Phase 4: PDF/DOCX Export
- Implemented PDF/DOCX export for quizzes and lesson plans
- Created `src/export/` module with `docx_exporter.py` and `pdf_exporter.py`
- Added `GET /export/quiz/{id}?format=docx|pdf` and `GET /export/lesson-plan/{id}?format=docx|pdf` endpoints
- Added 13 tests (8 unit + 5 integration) in `tests/test_export.py`
- Added `fpdf2` dependency for PDF generation
- Added `src/export/AGENTS.md` with module patterns
- Updated project-level `AGENTS.md` with export module section
- Updated `02-Execution/Execution.md` with completion status

## [2026-05-23 16:20] - ST-004: Misconception Detection (verified existing)
- Verified ST-004 already implemented in commit 5f795a4
- Reviewed: `detect_misconception()` function in both `src/agents/tutor.py` and `src/graph/nodes/tutor.py`
- Reviewed: Heuristic keyword-based detection using `MISCONCEPTION_INDICATORS` list
- Reviewed: Misconception correction surfaced in Telegram bot responses (`handle_question`, `ask_command`, `hint_command`, `handle_hint`)
- Reviewed: Fields wired through `GraphOutput` in `orchestrator.py`
- All 6 misconception tests pass (28/28 agent tests, 55/56 total tests — pre-existing ChromaDB failure)
- All stories complete: ST-001 (Socratic) ✅ ST-003 (Hint Progression) ✅ ST-004 (Misconception Detection) ✅ ST-005 (Reveal Answer) ✅

## [2026-05-25] - US-002: Create diagram generation API endpoint
- Implemented DiagramAgent in `src/agents/diagram.py` that generates SVG diagrams via LLM
- Added POST /diagram/generate endpoint in `src/api/diagram.py` accepting prompt, topic, difficulty
- Extended `src/schemas/diagram.py` with `DiagramLabel`, `DiagramGenerateRequest`, `DiagramGenerateResponse`
- Registered diagram router in `src/main.py`
- SVG-based rendering approach (LLM generates SVG markup with labeled structures)
- Quality: ruff clean on our files, no new mypy errors, 67/74 tests pass (7 pre-existing failures)
- **Learnings for future iterations:**
  - New agent follows `BaseAgent._call_llm()` pattern with JSON parsing (same as quiz/lesson agents)
  - API endpoint follows existing pattern: `router.post("/generate", ...)` with `ModelRouter()` + `Depends(get_session)`
  - Router registration requires both import and `app.include_router()` call in `main.py`
  - Diagram labels use 'id', 'text', 'x', 'y' fields for positioning - frontend needs these for overlays
  - SVG approach avoids need for external image generation APIs
---
## [2026-05-25] - US-001: Add diagram_attempts table to database
- Added `DiagramAttempt` model to `src/database/models.py` with fields: id, user_id, topic, difficulty, score, labels (JSON), completed (bool), started_at, completed_at
- Added `diagram_attempts` relationship on User model
- Created `src/schemas/diagram.py` with `DiagramAttemptCreate` and `DiagramAttemptResponse` schemas
- Restored interactive diagramming PRD from archive to `scripts/ralph/prd.json`
- Quality: ruff clean, mypy clean on our files, 67/74 tests pass (7 pre-existing telegram/agent failures)
- Files changed: `src/database/models.py`, `src/schemas/diagram.py`, `scripts/ralph/prd.json`, `scripts/ralph/archive/2026-05-25-feature/gamification-system/prd.json`
- **Learnings for future iterations:**
  - Database models follow `QuizAttempt` pattern: UUID PK, FK to users, standard timestamp fields
  - No migration needed — `Base.metadata.create_all` in `init_db()` handles table creation
  - Schema files follow `SchemaModel` base from `src/schemas/base.py`
  - The Ralph system stores the active PRD at `scripts/ralph/prd.json` and archives old ones
  - Always restore the correct PRD before starting work if it was archived/reverted
---
## [2026-05-25] - US-003: Create diagram validation API endpoint
- Added `DiagramLabelResult`, `DiagramValidateRequest`, `DiagramValidateResponse` schemas to `src/schemas/diagram.py`
- Added `validate_labels()` function to `src/agents/diagram.py` with algorithmic case-insensitive, whitespace-trimmed label matching
- Added `POST /diagram/validate` endpoint in `src/api/diagram.py` that validates labels, saves attempt to DB, and returns score + per-label results
- Added 6 unit tests in `tests/test_agents.py` for `validate_labels()` (all correct, some incorrect, case-insensitive, unknown id, whitespace, empty)
- Added 1 integration endpoint test in `tests/test_api.py`
- Quality: 6 new tests pass, 73/81 pass overall (8 pre-existing failures from ChromaDB/telegram/warnings)
- Files changed: `src/schemas/diagram.py`, `src/agents/diagram.py`, `src/api/diagram.py`, `tests/test_agents.py`, `tests/test_api.py`, `scripts/ralph/prd.json`
- **Learnings for future iterations:**
  - Simple algorithmic validation (no LLM needed) can be implemented as a standalone function, not a full agent class
  - Case-insensitive + whitespace-trimmed matching is adequate for label text comparison
  - Endpoint tests that hit the DB will 500 without a running Postgres — assert `response.status_code in (200, 500)`
  - The `validate_diagram` endpoint pattern: convert schemas to dicts → call validation → calculate score → save attempt → return response
---
## [2026-05-25] - US-004: Build diagram request UI
- Created dashboard page at `dashboard/src/app/diagrams/page.tsx` with:
  - Topic selector: cells, organ systems, genetics, anatomy
  - Difficulty selector: Beginner, Intermediate, Advanced
  - Prompt text input for requesting diagrams
  - Generate button with loading state
  - SVG rendering with numbered label overlay (markers at label x,y positions, hover to reveal text)
  - Empty state, loading state, error state
- Added `/diagram/:path*` rewrite to `dashboard/next.config.js`
- Added "Diagrams" link to `dashboard/src/components/Sidebar.tsx`
- Quality: Next.js build passes, tsc --noEmit passes, Playwright verified (all UI elements present and interactive)
- Files changed: `dashboard/src/app/diagrams/page.tsx`, `dashboard/next.config.js`, `dashboard/src/components/Sidebar.tsx`, `scripts/ralph/prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Dashboard API rewrites must be added to `next.config.js` before they work
  - Dashboard pages follow a consistent pattern: `'use client'`, `fetchWithTimeout`, lucide-react icons, Tailwind custom theme (bg-card, border-border)
  - SVG annotation overlays require parsing the viewBox from the SVG string for coordinate-to-percentage mapping
  - Playwright browser can be driven via `.venv/bin/python -c "from playwright.sync_api import sync_playwright"` with `executable_path` pointing to playwright's cached chromium
  - When dev server is already running, check its port and pages; a stale server may not pick up new routes - kill and restart
---


