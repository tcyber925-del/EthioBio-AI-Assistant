name: Build & push Render image
on:
  push:
    branches: [ main ]
  workflow_dispatch:
concurrency:
  group: render-image-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      RENDER_DEPLOY_HOOK_URL: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
      IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/ethiobio-ai-assistant
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ env.IMAGE_NAME }}:latest,${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Trigger Render redeploy
        if: github.event_name == 'push' && env.RENDER_DEPLOY_HOOK_URL != ''
        run: curl -sSf -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
