name: Keep-Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sSf --max-time 30 -o /dev/null "https://ethiobio-api.onrender.com/liveness"
