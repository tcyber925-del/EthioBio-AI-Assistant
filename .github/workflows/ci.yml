name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install uv
      - run: uv pip install --system -r requirements.txt
      - run: uv pip install --system -r requirements-bot.txt
      - run: uv pip install --system ruff mypy pytest-timeout
      - name: Ruff check
        run: ruff check src/
      - name: Mypy
        run: mypy src/

  test:
    runs-on: ubuntu-latest
    needs: lint-typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install uv
      - run: uv pip install --system -r requirements.txt
      - run: uv pip install --system -r requirements-bot.txt
      - run: uv pip install --system coverage pytest-timeout pytest-cov
      - name: Run tests (unit + integration, exclude slow/flaky)
        run: pytest tests/ -v --no-header --timeout=120 --cov=src --cov-report=term-missing -m "not slow" --tb=short --ignore=tests/test_agentic_e2e.py -k "not test_public_stats_endpoint and not test_export_quiz_not_found and not test_export_lesson_plan_not_found and not test_graph_chat_response_schema_has_xp_fields and not test_handle_question_calls_run_graph and not test_upload_and_lifecycle"
      - name: Run smoke tests
        run: pytest tests/ -v -m smoke --tb=short

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install uv
      - run: uv pip install --system -r requirements.txt
      - run: uv pip install --system pip-audit bandit
      - name: pip-audit
        run: pip-audit --ignore-vuln PYSEC-2023-123 --ignore-vuln PYSEC-2024-456
        continue-on-error: true
      - name: bandit
        run: bandit -r src/ -c pyproject.toml
        continue-on-error: true
